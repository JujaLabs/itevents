subprojects {

    apply plugin: 'java'

    repositories {
        mavenCentral()
        mavenLocal()
    }

    sourceCompatibility = 1.7
    targetCompatibility = 1.7
}

defaultTasks 'clean', 'assemble', 'liquibaseUpdate', 'test'